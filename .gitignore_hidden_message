 Must fix (blocking)
  1. Fill in real values in secrets/db_password.txt, secrets/db_root_password.txt, and un-comment + fill
     SQL_PASSWORD/SQL_ROOT_PASSWORD in srcs/.env (or move these two to secrets as well, for consistency).
  2. Un-comment + fill WP_ADMIN_PASSWORD and WP_SECOND_PASSWORD in srcs/.env — better yet, add them as Docker secrets like you did
     for the DB password, and update wordpress/conf/auto_config.sh to read them from /run/secrets/....
  3. If you go the secrets route for the WP passwords, wire credentials.txt into docker-compose.yml's secrets: section (or delete
     it if unused).

  Git hygiene (do before putting real passwords anywhere)
  4. Add srcs/.env to .gitignore; commit an srcs/.env.example with placeholder values instead.
  5. Run git rm --cached secrets/db_password.txt secrets/db_root_password.txt secrets/credentials.txt so the existing .gitignore
  rule actually takes effect on them.

  Verify / likely fix
  6. Check whether nginx is still serving the Debian default site on port 80 inside the container (docker exec nginx nginx -T, or
  curl port 80 from another container on the network). If so, remove/disable /etc/nginx/sites-enabled/default in the nginx
  Dockerfile.

  Cleanup
  7. Dedupe srcs/.env (repeated SQL_DATABASE, SQL_USER, SQL_PASSWORD/SQL_ROOT_PASSWORD keys) and pick one spelling for the domain
  var (DOMAINE_NAME vs DOMAIN_NAME) everywhere it's used (.env, nginx/tools/entrypoint.sh, wordpress/conf/auto_config.sh).
  8. Change --url="$DOMAINE_NAME" to --url="https://$DOMAINE_NAME" in auto_config.sh to avoid mixed-content/redirect issues.
  9. Delete or fix execute.sh at the repo root — it references a root Dockerfile that doesn't exist and doesn't match your actual
  multi-container setup.
  10. Drop the obsolete version: '3.8' line from srcs/docker-compose.yml (Compose v2 warns on it).
  11. Fill in README.md / DEV_DOC.md / USER_DOC.md — currently empty stubs, and you already listed the topics an evaluator would
  likely ask about (secrets vs env vars, volumes vs bind mounts).
  12. Optional: drop the redundant wp_user@'localhost' account creation in mariadb/tools/entryscript.sh since WordPress only ever
  connects as wp_user@'%'.
